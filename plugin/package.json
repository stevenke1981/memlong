{
  "name": "ams-plugin",
  "version": "1.0.0",
  "description": "TypeScript shim for the OpenCode Agent Long-Term Memory System",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.cjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
